Create a Go app in a docker container to be deployed on a coolify instance that will receive a webhook call from Resend from a email.received event. Call back out to Resend with the email id to retrieve the email content in plain text. Parse the email and strip it down to the bodies topmost forwarded message or top message, ignoring the quoted text that comprises the entirety of the email chain. I am open to smarter parsing if that is an option, but i really just want to capture that forwarded bit. Then send that bit off to my twenty instance and create a ticket. We can work out the structure of the ticket object later, as i might want to add a field or two for this process. But for now, workout the resend api communication and the start of the twenty communication. I assume we will need env variables for twenty, and an env variable for resends api, I was also told i should have a key to verify the initial contact from resend, but i am not sure where to configure that in resend.
